import 'package:flutter/material.dart';

class TlalocChart extends StatelessWidget {
  const TlalocChart({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
