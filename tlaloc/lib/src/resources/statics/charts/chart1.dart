import 'package:flutter/material.dart';

class TlalocChart extends StatelessWidget {
  const TlalocChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
